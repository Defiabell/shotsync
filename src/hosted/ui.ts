import type { HostedEnv } from './types';
import { MOBILE_HEAD } from './mobile';

/** The hosted app intentionally has no service worker or persistent browser storage. */
export function hostedHTML(env: HostedEnv): string {
  const config = JSON.stringify({ sitekey: env.TURNSTILE_SITE_KEY || '' }).replace(/</g, '\\u003c');
  return `<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="referrer" content="no-referrer"><meta name="robots" content="noindex,nofollow">${MOBILE_HEAD}<title>ShotSync · 随手传，随处取</title><style>
:root{color-scheme:dark;font-family:-apple-system,system-ui,sans-serif;background:#111;color:#eee}*{box-sizing:border-box}body{margin:0;padding:env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left)}main{margin:auto}header{display:flex;align-items:center;padding:14px}.brand{font-size:16px;font-weight:700}.brand span{color:#eee}h1{font-size:30px}h2{font-size:17px;margin:0}p{line-height:1.6}small,.muted{color:#999;font-size:12px}a{color:#9dbdff}.card{background:#181818;border:1px solid #303030;border-radius:12px;padding:22px;margin:20px 0}.auth{max-width:480px;margin:36px auto;padding:0 18px}label{display:block;font-size:14px;margin:16px 0 7px}input,textarea,button{font:inherit}input,textarea{width:100%;background:#202020;border:1px solid #3a3a3a;border-radius:8px;color:inherit;padding:10px}textarea{resize:vertical;min-height:220px}button,.button{cursor:pointer;border:0;background:#333;color:#eee;border-radius:8px;padding:8px 12px;font-size:14px}button:hover{filter:brightness(1.2)}button:disabled{opacity:.5;cursor:wait}button:focus-visible{outline:2px solid #8aafff;outline-offset:3px}.primary{background:#2b6cff;color:#fff}.plain{background:transparent;color:#afc9ff}.row{display:flex;align-items:center;flex-wrap:wrap;gap:8px}.spread{justify-content:space-between}.toolbar{position:sticky;top:env(safe-area-inset-top);z-index:2;display:flex;align-items:center;gap:8px;padding:10px 14px;background:#181818;border-bottom:1px solid #2a2a2a}.toolbar .brand{flex:1}.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(120px,1fr));gap:6px;padding:6px}.tile{min-width:0}.tile.selected .tile-open{outline:3px solid #6798ff;outline-offset:-3px}.selecting .tile-open:after{content:"";position:absolute;right:8px;top:8px;width:20px;height:20px;border:2px solid #eee;border-radius:50%;background:#1119}.selecting .tile.selected .tile-open:after{content:"✓";background:#2b6cff;color:white;text-align:center;font-size:13px}.toolbar button{white-space:nowrap}.tile-open{display:block;width:100%;aspect-ratio:1;overflow:hidden;padding:0;background:#202020;border-radius:6px;text-align:left;position:relative}.preview{width:100%;height:100%;object-fit:cover;display:block}.textpreview{white-space:pre-wrap;overflow:hidden;padding:12px;height:100%;background:#1c2030;color:#cdd3e0;font-size:12px;overflow-wrap:anywhere}.placeholder{display:flex;align-items:center;justify-content:center;height:100%;color:#999;font-size:12px;padding:10px}.tile-label{position:absolute;bottom:0;left:0;right:0;background:linear-gradient(transparent,#000b);padding:18px 8px 6px;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.notice{position:fixed;z-index:8;bottom:max(18px,env(safe-area-inset-bottom));left:50%;transform:translateX(-50%);max-width:calc(100% - 24px);width:max-content;background:#303030;border-radius:12px;padding:10px 16px;font-size:13px;overflow-wrap:anywhere;box-shadow:0 4px 24px #0006}.error{background:#552d34;color:#ffd4dc}[hidden]{display:none!important}.empty{padding:90px 20px;text-align:center;color:#888;font-size:14px}.device{padding:12px 0;border-bottom:1px solid #303030}.token{overflow-wrap:anywhere;user-select:all;white-space:pre-wrap}.danger{color:#ffb4be}.tabs{margin-bottom:20px}.meters{display:grid;grid-template-columns:1fr 1fr;gap:14px}progress{width:100%;height:6px;accent-color:#2b6cff}dialog{color:#eee;background:#181818;border:1px solid #333;border-radius:14px;padding:20px;width:min(560px,calc(100% - 24px));max-height:calc(100dvh - 32px);overflow:auto}dialog::backdrop{background:#000b}dialog h2{margin:0}.dialog-head{margin-bottom:20px}#viewer-dialog{width:min(900px,calc(100% - 24px))}#viewer-content{display:flex;justify-content:center;min-height:120px;margin:14px 0}#viewer-content img{max-width:100%;max-height:62dvh;object-fit:contain}#viewer-content pre{width:100%;margin:0;white-space:pre-wrap;overflow-wrap:anywhere;font:14px/1.6 ui-monospace,monospace;max-height:60dvh;overflow:auto}#viewer-name{overflow-wrap:anywhere}#share-result{margin-top:16px}#new-token{background:#242c3c;border-radius:8px;padding:12px;margin:12px 0}#settings-dialog section{margin-top:24px}#settings-dialog .account-email{overflow-wrap:anywhere;margin:12px 0}body.dragging:after{content:'松开即可上传图片';position:fixed;inset:12px;z-index:10;border:2px dashed #6798ff;border-radius:14px;background:#15284bea;display:grid;place-items:center;pointer-events:none}@media(max-width:520px){.toolbar{gap:6px;padding:10px}.toolbar button{padding:8px 10px}.grid{grid-template-columns:repeat(3,minmax(0,1fr))}dialog{padding:16px}.toolbar .brand{display:none}.toolbar{justify-content:flex-end;flex-wrap:wrap}.auth{margin-top:20px}}
</style></head><body><main><header id="auth-brand"><div class="brand">Shot<span>Sync</span></div></header><div id="status" class="notice" role="status" aria-live="polite" hidden></div>
<section id="auth" class="auth"><h1>随手传，随处取。</h1><p class="muted">这里是公共账号服务。想把内容放在自己的 Cloudflare？<a href="https://github.com/Defiabell/shotsync/blob/main/README.zh-CN.md#自己部署约-5-分钟">自己部署，无需注册账号</a>。</p><p class="muted">让截图和文字，在手机与电脑之间轻松流动。免费试用限 100 个账号，新注册账号的内容默认保留 7 天。</p><p><a id="auth-mobile-guide" href="/mobile">手机使用：添加到主屏幕 / 快捷指令 →</a></p><div id="auth-card" class="card"><div class="row tabs"><button id="tab-login" class="primary">登录</button><button id="tab-register">注册</button></div><h2 id="auth-title">欢迎回来</h2><form id="auth-form"><p class="muted">邮箱仅作为登录名，不验证邮箱，也不会发送找回密码邮件。请保存注册后显示的恢复码。</p><label for="email">邮箱</label><input id="email" type="email" autocomplete="email" maxlength="254" required><div id="recovery-wrap" hidden><label for="recovery-code">恢复码</label><input id="recovery-code" type="text" autocomplete="off" maxlength="128" spellcheck="false"></div><div id="password-wrap"><label for="password" id="password-label">密码</label><input id="password" type="password" autocomplete="current-password" minlength="10" maxlength="72" required><small>至少 10 个字符；最多 72 个英文字符或 24 个汉字</small></div><div id="captcha" hidden style="margin-top:16px"></div><button class="primary" id="auth-submit" style="width:100%;margin-top:20px">登录</button></form><div class="row" style="margin-top:16px"><button id="forgot" class="plain">用恢复码重置密码</button></div></div><div id="recovery-result" class="card" hidden><h2>请保存你的恢复码</h2><p>此恢复码仅显示一次，用于重置密码。请存到密码管理器等安全位置，不要交给他人。</p><p class="muted">如果密码和恢复码都丢失，将无法自行找回账号。重置密码后，旧恢复码立即失效。</p><code id="recovery-value" class="token"></code><div class="row" style="margin-top:16px"><button id="copy-recovery">复制恢复码</button></div><label><input id="recovery-saved" type="checkbox" style="width:auto"> 我已安全保存恢复码</label><button id="finish-recovery" class="primary" disabled>继续登录</button></div></section>
<section id="app" hidden><div class="toolbar"><div class="brand">shotsync</div><button id="add-text">文字</button><button id="add-image" class="primary">+ 图片</button><button id="select-items">选择</button><button id="delete-selected" class="danger" hidden disabled>删除选中 (0)</button><button id="cancel-selection" hidden>取消</button><button id="refresh" aria-label="刷新内容" title="刷新内容">↻</button><button id="open-settings" aria-label="设置" title="设置">⚙</button><input id="file" type="file" accept="image/jpeg,image/png,image/webp" multiple hidden></div><div id="gallery" class="grid"></div><div id="empty" class="empty" hidden>传一张图片，或发一段文字。<p class="muted">另一台设备登录，即可取回。也可以拖入或粘贴图片。</p></div></section>
<dialog id="composer-dialog" aria-labelledby="composer-title"><div class="row spread dialog-head"><h2 id="composer-title">发送文字</h2><button id="close-composer" aria-label="关闭文字输入">关闭</button></div><form id="text-form"><textarea id="text" aria-label="同步文字" placeholder="粘贴或输入文字，在另一台设备取回……" required></textarea><div class="row spread" style="margin-top:12px"><span class="muted">最多 100 KiB</span><button class="primary">发送</button></div></form></dialog>
<dialog id="viewer-dialog" aria-labelledby="viewer-name"><div class="row spread"><h2 id="viewer-name"></h2><button id="close-viewer">关闭</button></div><p id="viewer-meta" class="muted"></p><div id="viewer-content"></div><div class="row"><button id="viewer-copy" class="primary" hidden>复制文字</button><button id="viewer-download">取回</button><button id="viewer-share">分享</button><button id="viewer-revoke">撤销分享</button><button id="viewer-delete" class="danger">删除</button></div><div id="share-result" hidden><input id="share-link" readonly aria-label="分享链接"><p class="muted">任何持有链接的人都可访问，最多 24 小时、最多 50 次；可随时撤销。</p><button id="copy-share">复制链接</button></div></dialog>
<dialog id="settings-dialog" aria-labelledby="settings-title"><div class="row spread dialog-head"><h2 id="settings-title">设置</h2><button id="close-settings">关闭</button></div><p><a class="button primary" id="mobile-guide" href="/mobile" target="_blank" rel="noopener" style="display:inline-block;text-decoration:none">📱 手机快捷入口设置 →</a></p><p class="muted">添加到主屏幕，或设置快捷指令，以后点图标就能打开。</p><p id="identity" class="account-email"></p><p id="retention" class="muted"></p><div id="meters" class="meters"></div><p class="muted">每日额度按 UTC 零点重置。图片最多 10 MiB，文字最多 100 KiB；每分钟最多上传 10 次。删除不返还当天上传额度。</p><section><h2>连接其他设备</h2><label for="gallery-url">相册地址</label><input id="gallery-url" readonly><button id="copy-gallery-url" style="margin-top:8px">复制相册地址</button><p class="muted">其他浏览器打开相册地址后登录。Mac 客户端和快捷指令使用此地址与设备令牌连接。</p><p class="muted">本次页面中新建的令牌可再次查看、复制；刷新页面或退出后无法再次查看，请保存到密码管理器。服务端仅保存令牌哈希，已有令牌无法恢复，可撤销后重新生成。</p><form id="device-form" class="row"><input id="device-name" maxlength="60" required placeholder="设备名称，例如：我的 Mac" aria-label="设备名称" style="flex:1;min-width:150px"><button>生成令牌</button></form><div id="new-token" hidden><p id="token-label">设备令牌</p><p class="muted">请复制并安全保存；刷新页面或退出后无法再次查看。</p><code id="token-value" class="token"></code><div class="row" style="margin-top:12px"><button id="reveal-token">显示令牌</button><button id="copy-token">复制令牌</button><button id="dismiss-token">我已保存，隐藏</button></div></div><div id="devices"></div></section><div class="row spread" style="margin-top:24px"><a class="muted" href="https://github.com/Defiabell/shotsync/blob/main/README.zh-CN.md#自己部署约-5-分钟">自己部署</a><button id="logout" hidden>退出登录</button></div></dialog>
</main><script>
'use strict';
const config=${config};
const $=id=>document.getElementById(id);
let mode='login',user=null,captchaId=null,captchaToken='',generation=0,recoveryPending=false,accessToken='',expiresAt=0,refreshPromise=null;
const blobs=new Set(),entries=new Map(),selected=new Set(),deviceSecrets=new Map();let selectMode=false,deleting=false,currentTokenId=null,tokenShown=false,deviceListVersion=0;let listPromise=null,pollTimer=null,pollFailures=0,previewActive=0,previewQueue=[],viewerId=null,viewerVersion=0,uploading=false,composerVersion=0,devicePending=false,noticeTimer=null;
let contentRequests=0;const contentWaiters=[];
async function fetchContent(id,stamp){if(contentRequests>=2)await new Promise(resolve=>contentWaiters.push(resolve));else contentRequests++;try{if(stamp!==generation)return null;return await api('/i/'+encodeURIComponent(id),{blob:true});}finally{const next=contentWaiters.shift();if(next)next();else contentRequests--;}}
const observer=new IntersectionObserver(records=>{for(const record of records)if(record.isIntersecting){observer.unobserve(record.target);queuePreview(record.target.dataset.id);}},{rootMargin:'180px'});
const errors={'Invalid email or recovery code':'邮箱或恢复码不正确。','Recovery code already used':'恢复码已使用，请使用最新保存的恢复码。','Account registration and recovery are temporarily unavailable':'注册和账号恢复暂时不可用，请稍后重试。','Account already exists. Sign in or use your recovery code.':'账号已存在，请登录或使用恢复码重置密码。','Account already exists or trial is full.':'账号已存在或试用名额已满。','Trial is full. Please try again later.':'试用名额已满，请稍后再来。','Try again later':'操作太频繁，请稍后重试。','Invalid recovery code':'邮箱或恢复码不正确。','Maximum 10 active devices':'最多添加 10 台设备，请先撤销不再使用的令牌。',unauthorized:'登录已过期，请重新登录。',beta_full:'试用名额已满，请稍后再来。',quota_exceeded:'额度已用完，请删除旧内容或等待每日额度重置。',rate_limited:'操作太频繁，请稍后重试。',invalid_credentials:'邮箱或密码不正确。',invalid_recovery_code:'邮箱或恢复码不正确。'};
function notice(message,error=false){clearTimeout(noticeTimer);const dialog=document.querySelector('dialog[open]');(dialog||document.querySelector('main')).append($('status'));$('status').textContent=message;$('status').classList.toggle('error',error);$('status').hidden=!message;if(message&&!error){const stamp=generation;noticeTimer=setTimeout(()=>{if(stamp===generation)$('status').hidden=true;},4000);}}
function clearPrivate(){generation++;stopPolling();selected.clear();selectMode=false;deleting=false;renderSelection();deviceSecrets.clear();currentTokenId=null;tokenShown=false;renderToken();observer.disconnect();previewQueue=[];entries.clear();viewerId=null;viewerVersion++;composerVersion++;notice('');for(const id of ['settings-dialog','composer-dialog','viewer-dialog'])$(id).close();$('viewer-content').replaceChildren();$('viewer-name').textContent='';$('viewer-meta').textContent='';$('share-link').value='';$('share-result').hidden=true;$('auth-brand').hidden=false;document.body.classList.remove('dragging');accessToken='';expiresAt=0;user=null;recoveryPending=false;$('recovery-value').textContent='';$('recovery-code').value='';$('recovery-result').hidden=true;$('auth-card').hidden=false;for(const url of blobs)URL.revokeObjectURL(url);blobs.clear();for(const id of ['gallery','devices','meters'])$(id).replaceChildren();$('token-value').textContent='';$('new-token').hidden=true;$('identity').textContent='';$('text').value='';$('file').value='';$('device-name').value='';$('app').hidden=true;$('auth').hidden=false;$('logout').hidden=true;}
function saveSession(data){if(typeof data.accessToken!=='string'||!data.accessToken||!Number.isFinite(data.expiresAt))throw new Error('登录响应无效，请重试。');accessToken=data.accessToken;expiresAt=data.expiresAt;}
async function refreshSession(){if(refreshPromise)return refreshPromise;const stamp=generation;refreshPromise=(async()=>{const response=await fetch('/api/account/refresh',{method:'POST',headers:{'Content-Type':'application/json'},body:'{}',credentials:'same-origin',cache:'no-store'});if(!response.ok){const error=new Error([401,409].includes(response.status)?'登录已过期，请重新登录。':'登录服务暂时不可用，请稍后重试。');error.authInvalid=[401,409].includes(response.status);throw error;}const data=await response.json();if(stamp!==generation)throw new Error('登录状态已改变，请重试。');saveSession(data);return data;})();try{return await refreshPromise;}finally{refreshPromise=null;}}
async function api(path,options={}){const stamp=generation;const publicRequest=['/api/account/login','/api/account/register','/api/account/reset-password'].includes(path);if(!publicRequest&&(!accessToken||Date.now()>=expiresAt-30000)){try{await refreshSession();}catch(error){if(stamp===generation&&error.authInvalid){clearPrivate();setMode('login');}throw error;}}const send=()=>{if(stamp!==generation)throw new Error('登录状态已改变，请重试。');const headers=new Headers(options.headers);if(!publicRequest&&accessToken)headers.set('Authorization','Bearer '+accessToken);return fetch(path,{credentials:'same-origin',cache:'no-store',...options,headers});};const usedToken=accessToken;let response=await send();if(stamp!==generation)throw new Error('登录状态已改变，请重试。');if(response.status===401&&!publicRequest){try{if(accessToken===usedToken)await refreshSession();response=await send();}catch(error){if(stamp===generation&&error.authInvalid){clearPrivate();setMode('login');}throw error;}}if(response.status===401){if(stamp===generation){clearPrivate();setMode('login');}throw new Error(path==='/api/account/login'?'邮箱或密码不正确。':'登录已过期，请重新登录。');}if(!response.ok){let body={};try{body=await response.json();}catch{}const retry=response.headers.get('retry-after');throw new Error((errors[body.error]||body.error||('请求失败（'+response.status+'）'))+(retry?'；请在 '+retry+' 秒后重试':''));}if(options.blob)return response.blob();return response.status===204?{}:response.json();}
const post=(path,data)=>api(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});
async function run(button,action){const stamp=generation;button.disabled=true;try{await action();}catch(error){if(stamp===generation||!user)notice(error.message,true);}finally{button.disabled=false;}}
function setMode(next){if(recoveryPending)return;mode=next;notice('');$('auth-title').textContent={login:'欢迎回来',register:'创建你的账号',reset:'用恢复码重置密码'}[mode];$('auth-submit').textContent={login:'登录',register:'注册并获取恢复码',reset:'重置密码并获取新恢复码'}[mode];$('password').value='';$('password').autocomplete=mode==='login'?'current-password':'new-password';$('password-label').textContent=mode==='reset'?'新密码':'密码';$('recovery-wrap').hidden=mode!=='reset';$('recovery-code').required=mode==='reset';$('recovery-code').value='';$('captcha').hidden=mode==='login';$('tab-login').classList.toggle('primary',mode==='login');$('tab-register').classList.toggle('primary',mode==='register');if(mode!=='login')mountCaptcha();}
function mountCaptcha(){if(!config.sitekey||!['register','reset'].includes(mode))return;if(window.turnstile&&captchaId===null)captchaId=window.turnstile.render('#captcha',{sitekey:config.sitekey,callback:token=>captchaToken=token,'expired-callback':()=>captchaToken='','error-callback':()=>{captchaToken='';notice('验证服务暂时不可用，请稍后重试。',true);}});}
function showRecovery(code){generation++;recoveryPending=true;$('password').value='';$('recovery-code').value='';$('recovery-value').textContent=code;$('auth-card').hidden=true;$('recovery-result').hidden=false;$('recovery-saved').checked=false;$('finish-recovery').disabled=true;notice('操作成功，请先安全保存恢复码。');}
window.addEventListener('beforeunload',event=>{if(recoveryPending){event.preventDefault();event.returnValue='';}});
$('copy-recovery').onclick=()=>run($('copy-recovery'),async()=>{const stamp=generation;await navigator.clipboard.writeText($('recovery-value').textContent);if(stamp!==generation)return;notice('恢复码已复制，请存到安全位置后再继续。');});
$('recovery-saved').onchange=()=>{$('finish-recovery').disabled=!$('recovery-saved').checked;};
$('finish-recovery').onclick=()=>{if(!$('recovery-saved').checked)return;recoveryPending=false;$('recovery-value').textContent='';$('recovery-result').hidden=true;$('auth-card').hidden=false;setMode('login');notice('请使用你的密码登录。');};
if(config.sitekey){const script=document.createElement('script');script.src='https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit';script.async=true;script.onload=mountCaptcha;document.head.append(script);}
$('tab-login').onclick=()=>setMode('login');$('tab-register').onclick=()=>setMode('register');$('forgot').onclick=()=>setMode('reset');
$('auth-form').onsubmit=event=>{event.preventDefault();run($('auth-submit'),async()=>{const submittedMode=mode;if(submittedMode==='login')generation++;const needsCaptcha=['register','reset'].includes(submittedMode);const email=$('email').value.trim(),password=$('password').value,recoveryCode=$('recovery-code').value.trim();if(new TextEncoder().encode(password).byteLength>72)throw new Error('密码过长：最多 72 字节（约 24 个汉字）。');const stamp=generation;const endpoint={login:'login',register:'register',reset:'reset-password'}[submittedMode];if(needsCaptcha&&config.sitekey&&!captchaToken)throw new Error('请完成安全验证。');try{const result=await post('/api/account/'+endpoint,{email,password,recoveryCode,turnstileToken:captchaToken});if(stamp!==generation)return;$('password').value='';if(submittedMode==='login'){generation++;saveSession(result);await loadAccount();return;}$('email').value=email;showRecovery(result.recoveryCode);}finally{if(needsCaptcha){captchaToken='';if(captchaId!==null&&window.turnstile)window.turnstile.reset(captchaId);}}});};
$('logout').onclick=()=>run($('logout'),async()=>{generation++;stopPolling();const stamp=generation;const headers={'Content-Type':'application/json'};if(accessToken)headers.Authorization='Bearer '+accessToken;let response;try{response=await fetch('/api/account/logout',{method:'POST',headers,body:'{}',credentials:'same-origin',cache:'no-store'});}catch{if(stamp===generation){schedulePoll();notice('退出登录暂时失败，请重试。',true);}return;}if(stamp!==generation)return;if(!response.ok){schedulePoll();notice('退出登录暂时失败，请重试。',true);return;}clearPrivate();setMode('login');});
function bytes(value){return value>=1048576?(value/1048576).toFixed(1)+' MiB':(value/1024).toFixed(1)+' KiB';}
function meter(label,value,max,format=String){const box=document.createElement('div'),caption=document.createElement('div'),bar=document.createElement('progress');caption.className='muted';caption.textContent=label+'：'+format(value)+' / '+format(max);bar.max=max;bar.value=value;box.append(caption,bar);return box;}
function showUsage(usage,limits={}){const days=limits.retentionDays;$('retention').textContent='仅你的账号可访问。'+(days===0?'内容永久保存，不自动过期；仍受存储额度限制，请保留重要内容的备份。':Number.isInteger(days)&&days>0?'内容保留 '+days+' 天，具体到期时间见每条内容。':'具体到期时间见每条内容。');$('meters').replaceChildren(meter('已存内容',usage.storedItems||0,limits.storedItems||100),meter('存储空间',usage.storedBytes||0,limits.storedBytes||209715200,bytes),meter('今日上传',usage.dailyUploads||0,limits.dailyUploads||50),meter('今日流量',usage.dailyBytes||0,limits.dailyBytes||104857600,bytes));}
function button(label,action){const node=document.createElement('button');node.textContent=label;node.onclick=()=>run(node,action);return node;}
function renderSelection(){for(const id of ['add-text','add-image','select-items'])$(id).hidden=selectMode;$('delete-selected').hidden=!selectMode;$('cancel-selection').hidden=!selectMode;$('delete-selected').textContent='删除选中 ('+selected.size+')';$('delete-selected').disabled=deleting||selected.size===0;$('cancel-selection').disabled=deleting;$('logout').disabled=deleting;$('gallery').classList.toggle('selecting',selectMode);for(const [id,entry] of entries){entry.tile.classList.toggle('selected',selected.has(id));const open=entry.tile.querySelector('button');if(selectMode)open.setAttribute('aria-pressed',String(selected.has(id)));else open.removeAttribute('aria-pressed');}}
function exitSelection(){selectMode=false;selected.clear();renderSelection();}
$('select-items').onclick=()=>{selectMode=true;selected.clear();renderSelection();};$('cancel-selection').onclick=()=>{if(!deleting)exitSelection();};
$('delete-selected').onclick=async()=>{if(deleting||!selected.size||!confirm('删除选中的 '+selected.size+' 项？此操作无法撤销。'))return;const stamp=generation,ids=Array.from(selected);deleting=true;renderSelection();let count=0,failure='';try{for(const id of ids){if(stamp!==generation)return;try{await api('/api/img/'+encodeURIComponent(id),{method:'DELETE'});if(stamp!==generation)return;selected.delete(id);const entry=entries.get(id);if(entry){releaseEntry(entry);entries.delete(id);}count++;}catch(error){if(stamp!==generation)return;failure=error.message;}renderSelection();}if(stamp!==generation)return;try{await refresh(true);}catch(error){failure=error.message;}if(stamp!==generation)return;if(!selected.size)exitSelection();notice('已删除 '+count+' 项。'+(selected.size?'剩余 '+selected.size+' 项未删除，请重试。':'')+(failure?' '+failure:''),!!failure);}finally{if(stamp===generation){deleting=false;renderSelection();}}};
function releaseEntry(entry){observer.unobserve(entry.tile);entry.removed=true;if(entry.url){URL.revokeObjectURL(entry.url);blobs.delete(entry.url);}entry.tile.remove();}
function queuePreview(id){const entry=entries.get(id);if(!entry||entry.content||entry.loading||entry.queued)return;entry.queued=true;previewQueue.push(entry);pumpPreviews();}
function pumpPreviews(){while(previewActive<2&&previewQueue.length){const entry=previewQueue.shift();entry.queued=false;if(entry.removed||entries.get(entry.item.id)!==entry)continue;previewActive++;ensureContent(entry).catch(()=>{if(!entry.removed&&entries.get(entry.item.id)===entry)entry.preview.textContent='点开重试';}).finally(()=>{previewActive--;pumpPreviews();});}}
async function ensureContent(entry){if(entry.content)return entry.content;if(entry.loading)return entry.loading;const stamp=generation;entry.loading=(async()=>{const blob=await fetchContent(entry.item.id,stamp);if(!blob||stamp!==generation)return null;let text=null;if(entry.item.type==='text')text=await blob.text();if(stamp!==generation||entry.removed||entries.get(entry.item.id)!==entry)return null;entry.content={blob,text};entry.preview.replaceChildren();if(text!==null){entry.preview.className='textpreview';entry.preview.textContent=text;}else{entry.url=URL.createObjectURL(blob);blobs.add(entry.url);const img=document.createElement('img');img.className='preview';img.alt=entry.item.name||'图片';img.src=entry.url;entry.preview.className='preview';entry.preview.append(img);}return entry.content;})();try{return await entry.loading;}finally{entry.loading=null;}}
function stopPolling(){clearTimeout(pollTimer);pollTimer=null;}
function schedulePoll(){stopPolling();if(!user||document.hidden||pollFailures>=3)return;const stamp=generation;pollTimer=setTimeout(async()=>{if(stamp!==generation||!user)return;try{await refresh();pollFailures=0;}catch(error){if(stamp===generation){pollFailures++;notice(pollFailures>=3?'自动同步已暂停，请点击刷新重试。':error.message,true);}}if(stamp===generation)schedulePoll();},25000);}
async function refresh(force=false){if(listPromise&&listPromise.stamp===generation){if(!force)return listPromise.promise;const stamp=generation;try{await listPromise.promise;}catch{}if(stamp!==generation)return;return refresh();}const stamp=generation;const request=(async()=>{const data=await api('/api/list');if(stamp!==generation||!user)return;showUsage(data.usage||{},data.limits||{});const ids=new Set(data.items.map(item=>item.id));for(const [id,entry] of entries)if(!ids.has(id)){releaseEntry(entry);entries.delete(id);selected.delete(id);if(viewerId===id)closeViewer();}$('empty').hidden=data.items.length!==0;for(const item of data.items){let entry=entries.get(item.id);if(!entry){const tile=document.createElement('article');tile.className='tile';tile.dataset.id=item.id;const open=document.createElement('button');open.className='tile-open';open.setAttribute('aria-label','打开 '+(item.name||(item.type==='text'?'文字':'图片')));const preview=document.createElement('div');preview.className='placeholder';preview.textContent=item.type==='text'?'文字':'图片';open.append(preview);open.onclick=()=>{if(selectMode){if(deleting)return;if(selected.has(item.id))selected.delete(item.id);else selected.add(item.id);renderSelection();}else run(open,()=>openViewer(item.id));};tile.append(open);entry={item,tile,preview,content:null,loading:null,queued:false,removed:false,url:null};entries.set(item.id,entry);observer.observe(tile);}else entry.item=item;$('gallery').append(entry.tile);}renderSelection();})();listPromise={stamp,promise:request};try{await request;}finally{if(listPromise&&listPromise.promise===request)listPromise=null;}}
function closeViewer(){viewerId=null;viewerVersion++;$('viewer-dialog').close();$('viewer-content').replaceChildren();$('viewer-name').textContent='';$('viewer-meta').textContent='';$('share-link').value='';$('share-result').hidden=true;}
async function openViewer(id){const entry=entries.get(id);if(!entry)return;const stamp=generation,version=++viewerVersion;viewerId=id;$('viewer-name').textContent=entry.item.name||(entry.item.type==='text'?'文字':'图片');$('viewer-meta').textContent=bytes(entry.item.size)+' · '+(entry.item.expiresAt===null?'永久保存':new Date(entry.item.expiresAt).toLocaleString('zh-CN')+' 到期');$('viewer-content').textContent='正在加载…';$('share-link').value='';$('share-result').hidden=true;$('viewer-copy').hidden=entry.item.type!=='text';$('viewer-dialog').showModal();let content;try{content=await ensureContent(entry);}catch(error){if(stamp===generation&&version===viewerVersion){$('viewer-content').textContent='加载失败，请关闭后重试。';notice(error.message,true);}return;}if(!content||stamp!==generation||version!==viewerVersion||viewerId!==id)return;const preview=document.createElement(content.text===null?'img':'pre');if(content.text===null){preview.src=entry.url;preview.alt=entry.item.name||'图片';}else preview.textContent=content.text;$('viewer-content').replaceChildren(preview);}
function viewerAction(id,action){$(id).onclick=()=>run($(id),async()=>{const entry=entries.get(viewerId);if(!entry)return;const stamp=generation,version=viewerVersion;const valid=()=>stamp===generation&&version===viewerVersion&&viewerId===entry.item.id;await action(entry,valid);});}
viewerAction('viewer-copy',async(entry,valid)=>{const content=await ensureContent(entry);if(!valid()||!content)return;await navigator.clipboard.writeText(content.text);if(valid())notice('文字已复制。');});
viewerAction('viewer-download',async(entry,valid)=>{const content=await ensureContent(entry);if(!valid()||!content)return;const url=entry.url||URL.createObjectURL(content.blob);if(!entry.url){entry.url=url;blobs.add(url);}const anchor=document.createElement('a');anchor.href=url;anchor.download=entry.item.name||(entry.item.type==='text'?'text.txt':'image');anchor.click();});
viewerAction('viewer-share',async(entry,valid)=>{const result=await post('/api/share/'+encodeURIComponent(entry.item.id),{});if(!valid())return;$('share-link').value=result.url;$('share-result').hidden=false;});
viewerAction('viewer-revoke',async(entry,valid)=>{await api('/api/share/'+encodeURIComponent(entry.item.id),{method:'DELETE'});if(!valid())return;$('share-link').value='';$('share-result').hidden=true;notice('分享已撤销。');});
viewerAction('viewer-delete',async(entry,valid)=>{if(!confirm('删除这条内容？此操作无法撤销。'))return;await api('/api/img/'+encodeURIComponent(entry.item.id),{method:'DELETE'});if(!valid())return;closeViewer();await refresh(true);});
$('copy-share').onclick=()=>run($('copy-share'),async()=>{const stamp=generation;await navigator.clipboard.writeText($('share-link').value);if(stamp===generation)notice('链接已复制。');});
$('close-viewer').onclick=closeViewer;$('viewer-dialog').addEventListener('cancel',event=>{event.preventDefault();closeViewer();});
$('add-text').onclick=()=>{composerVersion++;$('composer-dialog').showModal();$('text').focus();};
function closeComposer(){composerVersion++;$('composer-dialog').close();$('text').value='';}
$('close-composer').onclick=closeComposer;$('composer-dialog').addEventListener('cancel',event=>{event.preventDefault();closeComposer();});
$('open-settings').onclick=()=>{$('gallery-url').value=location.origin+'/';tokenShown=false;renderToken();$('settings-dialog').showModal();run($('open-settings'),loadDevices);};
function closeSettings(){if(devicePending){notice('正在生成令牌，请稍候。');return;}$('settings-dialog').close();tokenShown=false;renderToken();}
$('close-settings').onclick=closeSettings;$('settings-dialog').addEventListener('cancel',event=>{event.preventDefault();closeSettings();});
function renderToken(){const saved=deviceSecrets.get(currentTokenId);$('new-token').hidden=!saved;$('token-value').textContent=saved?(tokenShown?saved.token:'••••••••••••••••'):'';$('token-label').textContent=saved?'设备令牌 · '+saved.name:'设备令牌';$('reveal-token').textContent=tokenShown?'隐藏令牌':'显示令牌';}
$('copy-gallery-url').onclick=()=>run($('copy-gallery-url'),async()=>{const stamp=generation;await navigator.clipboard.writeText(location.origin+'/');if(stamp===generation)notice('相册地址已复制。');});
async function loadDevices(){const stamp=generation,version=++deviceListVersion;const data=await api('/api/account/devices');if(stamp!==generation||version!==deviceListVersion||!user)return;const ids=new Set(data.devices.map(device=>device.id));for(const id of deviceSecrets.keys())if(!ids.has(id))deviceSecrets.delete(id);renderToken();$('devices').replaceChildren();for(const device of data.devices){const row=document.createElement('div');row.className='device row spread';row.dataset.deviceId=device.id;const name=document.createElement('span');name.textContent=device.name;row.append(name);if(deviceSecrets.has(device.id))row.append(button('查看令牌',async()=>{currentTokenId=device.id;tokenShown=false;renderToken();}));else{const note=document.createElement('small');note.textContent='无法再次查看，可撤销后重新生成';row.append(note);}row.append(button('撤销',async()=>{const stamp=generation;await api('/api/account/devices/'+encodeURIComponent(device.id),{method:'DELETE'});if(stamp===generation){deviceSecrets.delete(device.id);renderToken();await loadDevices();}}));$('devices').append(row);}}
async function loadAccount(){let stamp=generation;try{const data=await api('/api/account/me');if(stamp!==generation)return;if(!data.user){clearPrivate();return;}generation++;stamp=generation;user=data.user;$('auth').hidden=true;$('auth-brand').hidden=true;$('app').hidden=false;$('logout').hidden=false;$('identity').textContent=user.email;const activeGeneration=generation;await refresh();if(activeGeneration!==generation)return;pollFailures=0;schedulePoll();}catch(error){if(stamp===generation){notice(error.message,true);if(user){pollFailures=1;schedulePoll();}}}}
$('refresh').onclick=()=>run($('refresh'),async()=>{const stamp=generation;await refresh();if(stamp!==generation)return;pollFailures=0;schedulePoll();notice('已同步。');});
document.addEventListener('visibilitychange',()=>{if(document.hidden)stopPolling();else schedulePoll();});
async function uploadFiles(files){if(!user||uploading)return;const stamp=generation;uploading=true;$('add-image').disabled=true;try{let count=0;for(const file of files){if(stamp!==generation)return;if(!['image/jpeg','image/png','image/webp'].includes(file.type))throw new Error('支持 JPG、PNG 和 WebP 图片。');if(file.size>10485760)throw new Error('单张图片不能超过 10 MiB。');notice('正在上传 '+(count+1)+' / '+files.length+'…');const body=new FormData();body.append('full',file);await api('/api/upload',{method:'POST',body});if(stamp!==generation)return;count++;}if(stamp!==generation)return;await refresh(true);if(stamp===generation)notice('已同步 '+count+' 张图片。');}catch(error){if(stamp===generation){notice(error.message,true);try{await refresh(true);}catch{}}}finally{if(stamp===generation)$('file').value='';uploading=false;$('add-image').disabled=false;}}
$('add-image').onclick=()=>$('file').click();$('file').onchange=()=>uploadFiles(Array.from($('file').files));
let dragDepth=0;document.addEventListener('dragenter',event=>{if(!user||document.querySelector('dialog[open]')||!event.dataTransfer.types.includes('Files'))return;event.preventDefault();dragDepth++;document.body.classList.add('dragging');});document.addEventListener('dragover',event=>{if(user&&event.dataTransfer.types.includes('Files'))event.preventDefault();});document.addEventListener('dragleave',()=>{if(--dragDepth<=0){dragDepth=0;document.body.classList.remove('dragging');}});document.addEventListener('drop',event=>{dragDepth=0;document.body.classList.remove('dragging');if(!user)return;event.preventDefault();if(document.querySelector('dialog[open]'))return;const files=Array.from(event.dataTransfer.files);if(files.length)uploadFiles(files);});document.addEventListener('paste',event=>{if(!user||document.querySelector('dialog[open]'))return;const files=Array.from(event.clipboardData.files).filter(file=>file.type.startsWith('image/'));if(files.length){event.preventDefault();uploadFiles(files);}});
$('text-form').onsubmit=event=>{event.preventDefault();run(event.submitter,async()=>{const stamp=generation,version=composerVersion;const value=$('text').value;if(!value.trim())throw new Error('请输入文字。');if(new TextEncoder().encode(value).length>102400)throw new Error('文字不能超过 100 KiB。');const body=new FormData();body.append('full',new Blob([value],{type:'text/plain'}),'text.txt');await api('/api/upload',{method:'POST',body});if(stamp!==generation)return;if(version===composerVersion){closeComposer();notice('文字已同步。');}await refresh(true);});};
$('device-form').onsubmit=event=>{event.preventDefault();if(devicePending)return;devicePending=true;run(event.submitter,async()=>{try{const stamp=generation,name=$('device-name').value.trim();const result=await post('/api/account/devices',{name});if(stamp!==generation)return;deviceSecrets.set(result.id,{token:result.token,name});currentTokenId=result.id;tokenShown=false;renderToken();$('device-name').value='';await loadDevices();}finally{devicePending=false;}});};$('copy-token').onclick=()=>run($('copy-token'),async()=>{const stamp=generation,saved=deviceSecrets.get(currentTokenId);if(!saved)return;await navigator.clipboard.writeText(saved.token);if(stamp!==generation)return;notice('令牌已复制，请安全保存。');});$('reveal-token').onclick=()=>{tokenShown=!tokenShown;renderToken();};$('dismiss-token').onclick=()=>{currentTokenId=null;tokenShown=false;renderToken();};
const initialGeneration=generation;refreshSession().then(()=>{if(initialGeneration===generation)return loadAccount();}).catch(error=>{if(initialGeneration!==generation)return;if(error.authInvalid)clearPrivate();if(error.message!=='登录已过期，请重新登录。')notice(error.message,true);});
</script></body></html>`;
}
